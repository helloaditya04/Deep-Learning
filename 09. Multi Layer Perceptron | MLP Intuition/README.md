>1. Multi Layer Perceptron (MLP) is a typeof artificial neural network charchterised by its multiple layers of nodes(neurons).
>
>2. It consists of an input layer, one or more hidden layers, and an output layer.
